// import React, { useContext } from "react";
// import { AuthContext } from "../../context/AuthContext";

export default function UserDashboard() {
//   const { user } = useContext(AuthContext);

  return (
    <div>
      <h1>Only Admins can access the dashboard</h1>
    </div>
  );
}
